部署时的rpress

在项目中，我们可以通过`rpress server [... args]`的方式来一键生成Linux的部署文件
这是如何来完成的呢？
我们首先会将打包好的代码都copy到目标文件夹中，然后生成一个`app.js`，在该文件中，是对里面的服务的启动代码
我们在`app.js`中使用到了`sirv`来将所有的静态资源挂载，然后通过`child-process`来启动一个子进程，在子进程中我们下载`app.js`中需要的所有依赖